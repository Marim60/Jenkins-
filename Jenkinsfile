pipeline {
    agent any
    stages {
        // Or, if using PowerShell
        stage('Execute PowerShell Script') {
            steps {
                powershell 'execute_is_command.ps1'
            }
        }
    }
}
